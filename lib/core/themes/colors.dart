import 'package:flutter/material.dart';

class AppColors {
  static const backgroundLightGray = Color(0xFFF8F8F8); // Light background
  static const backgroundDarkBlue = Color(0xFF0F0F0F); // Main dark background
  static const cardBackground = Color(0xFFFFFFFF); // White background for cards
  static const textPrimary =
      Color(0xFF000000); // Black text for primary content
  static const textSecondary =
      Color(0xFF666666); // Lighter gray text for secondary content
  static const iconColor = Color(0xFF000000); // Icons in black for visibility
  static const buttonColor = Color(0xFFFE2C55); // Button color (TikTok Red)
  static const buttonTextColor = Color(0xFFFFFFFF); // White text on buttons
  static const borderColor =
      Color(0xFFE0E0E0); // Border color for input fields, cards
  static const errorColor = Color(0xFFFB4C5D); // Red color for error states
  static const dividerColor = Color(0xFFE0E0E0); // Divider line color
  static const shadowColor =
      Color(0x1F000000); // Light shadow color for UI elements
  static const successColor =
      Color(0xFF00C853); // Green color for success states
  static const inputFieldBackground =
      Color(0xFFF5F5F5); // Light background for input fields
  static const appBarBackground =
      Color(0xFFFFFFFF); // White background for the app bar
}
